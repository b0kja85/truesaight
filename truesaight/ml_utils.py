# import random

# def run_deepfake_model(video_path: str):
#     """
#     Dummy stub for ML inference. 
#     Returns a random (result, confidence, frames_count) tuple.
#     """
#     # 1. Randomly choose 'real' or 'fake'
#     result = random.choice(['real', 'fake'])
    
#     # 2. Random confidence between 0.5 and 1.0, rounded to 2 decimals
#     confidence = round(random.uniform(0.5, 1.0), 2)
    
#     # 3. Random frame count between 100 and 1000
#     frames_count = random.randint(100, 1000)
    
#     return result, confidence, frames_count

